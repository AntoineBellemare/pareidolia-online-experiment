# Pareidolia paper — Python dependencies (3.10+)

# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
pillow>=10.0

# Storage / I/O
pyarrow>=14.0          # parquet reader/writer

# NLP + embeddings
sentence-transformers>=2.2
hdbscan>=0.8.33
umap-learn>=0.5.4
nltk>=3.8

# Eye-tracking (I-DT fixation detection, gaze metrics)
# (uses only numpy + scipy; no extra dep)

# Manuscript builders
weasyprint>=60.0       # HTML → PDF preview

# Optional but useful
tqdm>=4.65
